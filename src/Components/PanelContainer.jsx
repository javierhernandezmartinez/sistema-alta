import "../Styles/Footer.scss"
const Footer = (props) => {
  return(
      <div className={"panel-container"}>
          {
              props.element
          }
      </div>

  )
}

export default Footer